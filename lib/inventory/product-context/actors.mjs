export function extractActors(projection = {}, contract = {}) { return { roles: [...new Set([...(projection.roles ?? []), ...(contract.declared?.roles ?? [])])].sort(), tenants: [...new Set([...(projection.tenants ?? []), ...(contract.declared?.tenants ?? [])])].sort() }; }
