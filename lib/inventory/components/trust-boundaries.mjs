export function trustBoundaries(components, relations = []) { return relations.filter((relation) => relation.trustBoundary === true).map((relation) => ({ ...relation, from: components.find((item) => item.id === relation.from)?.id ?? relation.from, to: components.find((item) => item.id === relation.to)?.id ?? relation.to })); }
