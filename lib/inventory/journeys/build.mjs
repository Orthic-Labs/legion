import { digest } from '../../core/binding.mjs';
export function buildJourneys({ portfolio, contract = {}, projection = {}, binding = null }) { const names = contract.declared?.criticalJourneys ?? projection.journeys ?? []; const journeys = portfolio.targets.flatMap((target) => names.map((name) => {const details=projection.journeyDetails?.[name]??{};return { id: `${target.id}:${name}`, targetId: target.id, name, origin: contract.declared?.criticalJourneys ? 'declared' : 'observed', criticality: details.criticality??null, actors:details.actors??[],steps:details.steps??[],invariants:details.invariants??[],dataEffects:details.dataEffects??[],externalDependencies:details.externalDependencies??[],failure:details.failure??[],recovery:details.recovery??[]};})); const gaps=names.length?[]:[{kind:'journey-denominator-missing',targetIds:portfolio.targets.map(({id})=>id)}];const value = { schemaVersion: 1, kind: 'nemesis-journey-inventory', journeys,gaps,complete:gaps.length===0, binding }; return { ...value, digest: digest(value) }; }
