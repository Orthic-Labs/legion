export function requireCapability(host, name, effect = 'blocked') { const capability = host.capabilities?.[name]; if (capability === true || capability?.active === true) return { available: true, receipt: capability?.receipt ?? null }; return { available: false, status: effect, capability: name, receipt: capability?.receipt ?? null }; }
