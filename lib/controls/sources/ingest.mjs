const TERMINAL=new Set(['mapped','merged-as-duplicate','partially-mapped','recommendation-only','product-decision','policy-dependent','rejected-as-non-universal','not-applicable-to-audit']);
export function ingestSource({ source, items }) { if (!/^sha256:[a-f0-9]{64}$/.test(source?.digest??'') || !source?.rightsStatus) throw new Error('source requires valid sha256 digest and rights status'); for(const item of items){if(!TERMINAL.has(item.disposition))throw new Error('source item requires terminal disposition');const rights=item.rightsStatus??source.rightsStatus;if((source.rightsStatus!=='cleared'||rights!=='cleared')&&(item.controlIds?.length||item.derivedContent))throw new Error('source item rights do not permit derived controls');} return { source:{...source,executionAuthority:false}, items: items.map((item, index) => ({ id: `${source.digest}:${index + 1}`, ...item,executionAuthority:false })) }; }
