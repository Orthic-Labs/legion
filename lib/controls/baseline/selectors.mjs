export function matchesSelector(selector = {}, { target, component, stacks = [], contract = {} }) { if (selector.op === 'always') return true; if (selector.targetKinds && !selector.targetKinds.includes(target?.kind)) return false; if (selector.componentKinds && !selector.componentKinds.includes(component?.kind)) return false; if (selector.facets && !selector.facets.some((facet) => target?.facets?.includes(facet))) return false; return true; }
