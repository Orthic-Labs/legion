import { readdir, readFile } from 'node:fs/promises';
import { join } from 'node:path';
import { validatePack } from '../contracts.mjs';
export async function loadControlPacks(root) { const index = JSON.parse(await readFile(join(root, 'registry/controls/packs/index.json'), 'utf8'));if(index.schemaVersion!==1||!Array.isArray(index.packs)||new Set(index.packs).size!==index.packs.length)throw new Error('invalid control pack index');const packs=await Promise.all(index.packs.map(async(path)=>validatePack(JSON.parse(await readFile(join(root,path),'utf8')))));const ids=new Set(packs.map(({id})=>id));for(const pack of packs)for(const dependency of pack.dependencies)if(!ids.has(dependency))throw new Error(`missing control pack dependency: ${pack.id}:${dependency}`);return packs; }
export function packIndex(packs) { const controls = new Map(); for (const pack of packs) for (const control of pack.controls) { const old = controls.get(control.id); if (old && JSON.stringify(old) !== JSON.stringify(control)) throw new Error(`incompatible duplicate control: ${control.id}`); controls.set(control.id, control); } return controls; }
