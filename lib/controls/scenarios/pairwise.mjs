export function pairwise(dimensions = {}) { const keys = Object.keys(dimensions).sort(); if (!keys.length) return []; return (dimensions[keys[0]] ?? []).map((value, index) => Object.fromEntries(keys.map((key) => [key, (dimensions[key] ?? [])[index % Math.max(1, dimensions[key].length)]]))); }
