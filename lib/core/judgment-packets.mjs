export function buildJudgmentPacket({ subject, evidence = [], omitted = [], reviewerRole, budget }) { return { schemaVersion: 1, subject: JSON.parse(JSON.stringify(subject ?? null)), evidence: [...evidence], omitted: [...omitted], reviewerRole, verdicts: ['confirmed','rejected','unproven','needs-human'], budget }; }
