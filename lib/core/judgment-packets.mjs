export function buildJudgmentPacket({ subject, evidence = [], omitted = [], reviewerRole, budget, lens = null }) { return Object.freeze({ schemaVersion: 1, subject: JSON.parse(JSON.stringify(subject ?? null)), evidence: [...evidence], omitted: [...omitted], reviewerRole, ...(lens ? { lens: Object.freeze({ ...lens }) } : {}), verdicts: ['confirmed','rejected','unproven','needs-human'], budget }); }
