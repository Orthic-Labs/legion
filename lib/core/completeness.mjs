export function reconcileClaim({ requiredEvidence = [], evidence = [] } = {}) { const available = new Set(evidence.map((item) => typeof item === 'string' ? item : item.id)); const gaps = requiredEvidence.filter((id) => !available.has(id)); return { complete: gaps.length === 0, status: gaps.length ? 'incomplete' : 'pass', gaps }; }
