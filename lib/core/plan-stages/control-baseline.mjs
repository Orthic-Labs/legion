import { compileBaseline } from '../../controls/baseline/compile.mjs';
import { evidenceCapabilities } from '../../controls/evidence/capabilities.mjs';
import { capabilityImpacts } from '../../controls/evidence/impacts.mjs';
export const controlBaselineStage = Object.freeze({ id: 'control-baseline', requiredFor: 'source', async run(options, host) { const topology = options.artifacts?.['product-portfolio'] ?? options.artifacts?.['product-topology'] ?? options.productInspection; if (!topology) return { complete: false, status: 'missing', detail: 'product-topology-required' };if(!(options.packs?.length))return{complete:false,status:'missing',detail:'control-packs-required',artifact:{baseline:null,impacts:[{kind:'control-denominator-missing'}]}}; const baseline = compileBaseline({ packs: options.packs, portfolio: topology.portfolio, components: topology.components, stacks: topology.stacks.stacks, contract: options.releaseContract ?? {}, binding: options.binding ?? null }); const impacts = capabilityImpacts(baseline, evidenceCapabilities(host));const denominator=(baseline.controls??[]).length;if(!denominator)return{complete:false,status:'unproven',detail:'control-denominator-zero',artifact:{baseline,impacts:[...impacts,{kind:'control-denominator-zero'}]}}; return { complete: impacts.length === 0, status: impacts.length ? 'unproven' : 'pass', artifact: { baseline, impacts }, detail: impacts.length ? 'evidence-capability-gap' : null }; } });
