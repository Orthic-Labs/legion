# Linux release contract uses GitHub Release tar.gz archives only.

channels:
  archive:
    format: tar.gz
    targets: [x64, arm64]
    contents:
      - dist/legion
      - dist/SHA256SUMS
      - dist/*.sbom.json
    notes:
      - "x64/arm64 artifacts pass install/uninstall and offline audit smoke tests."
