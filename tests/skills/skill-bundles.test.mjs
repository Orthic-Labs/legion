import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises'; import test from 'node:test'; import { validateSkillBundle } from '../../lib/skills/contracts.mjs';
for (const [id, count] of [['designer', 206], ['writing', 34]]) test(`${id} supplied bundle is complete and digest-bound`, async () => { const manifest = JSON.parse(await readFile(new URL(`../../skills/manifests/${id}.json`, import.meta.url), 'utf8')); validateSkillBundle(manifest); assert.equal(manifest.files.length, count); assert.ok(manifest.files.every((file) => file.sourceDigest.startsWith('sha256:') && file.outputDigest.startsWith('sha256:'))); });
