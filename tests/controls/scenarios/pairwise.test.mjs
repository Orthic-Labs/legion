import assert from 'node:assert/strict';import test from 'node:test';import {pairwise} from '../../../src/lib/controls/scenarios/pairwise.mjs';test('pairwise covers every pair',()=>{const rows=pairwise({a:[1,2],b:['x','y'],c:[true,false]});assert.ok([1,2].every((a)=>['x','y'].every((b)=>rows.some((row)=>row.a===a&&row.b===b))));});
