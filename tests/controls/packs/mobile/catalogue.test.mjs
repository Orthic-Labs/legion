import assert from 'node:assert/strict';import test from 'node:test';import value from '../../../../src/registry/controls/controls/mobile/core.json' with {type:'json'};test('mobile baseline separates iOS and Android',()=>assert.ok(value.controls.some(({id})=>id.endsWith('ios'))&&value.controls.some(({id})=>id.endsWith('android'))));
