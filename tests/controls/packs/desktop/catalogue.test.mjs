import assert from 'node:assert/strict';import test from 'node:test';import value from '../../../../registry/controls/controls/desktop/core.json' with {type:'json'};test('desktop baseline includes installer and updater',()=>assert.ok(value.controls.some(({id})=>id.endsWith('installer'))&&value.controls.some(({id})=>id.endsWith('updater-signing'))));
