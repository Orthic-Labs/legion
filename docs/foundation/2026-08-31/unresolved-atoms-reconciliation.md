# Unresolved atom reconciliation

Frozen product: `D:/Claude/legion-closure@8b022aa632f21aaa76f4625640b500dd9cdb4e74`
Foundation mode: `RECONCILE`; current-product operative-source review only; donor comparison excluded.

| Scope | Domain | Atom | Current product | Best observed | Recommended implementation | Material gap | Why / tradeoffs | Source evidence | Confidence |
|---|---|---|---|---|---|---|---|---|---|
| Legion current product | Work graph & execution coordination | LEG-017 — Resume interrupted execution from fingerprinted durable state without repeating completed effects. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. Reachable signed transition repair preserves binding/work state after interruption, but generic execution checkpoints + completed-effect replay guards lack a workflow-runtime consumer. | Current live mechanism: authenticated PREPARED/COMMITTED transition journal + `legion run repair` exact-byte recovery. | At workflow start, load one durable checkpoint bound to objective, intent epoch, repository, acceptance schedule, producer versions + event-chain fingerprint; restore completed effect IDs + outputs, reject drift, then schedule only unfinished dependency nodes with effect-ID deduplication. | Unresolved | Crash-safe contract transitions are observable, while full workflow continuation remains disconnected; promoting to DELIVERED would confuse narrow transition recovery with execution resumption. | Current product: `src/lib/cli/commands/run.mjs#runLifecycle`; Current product: `src/lib/cli/commands/governance/delivery/contract-lifecycle.mjs#ContractLifecycle.repair`; Current product: `src/lib/host/arcane/continuity.mjs#createExecutionCheckpoint`; Current product: `tests/arcane-package-contract-lifecycle.test.mjs#repair completes both pre-CAS & post-CAS crashes` (safeguard inspected, not run) + `tests/stage4-continuity.test.mjs#cancelled epochs deny stale work` | High |
| Legion current product | Work graph & execution coordination | LEG-018 — Pause at named planning or execution decisions & resume with captured operator direction. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. CLI suspend/supersede persists lifecycle state + sealed successor identity; budget lineage accepts authenticated user-turn resume evidence, but no named decision record captures prompt, options + chosen direction for continuation. | Current live mechanism: signed session suspension/supersession plus authenticated user-resume evidence at budget lineage boundary. | Persist pause receipt with decision ID, phase, admissible choices, context fingerprint + continuation token; bind operator response digest to chosen branch, advance continuation epoch, then resume exact suspended work or invalidate affected cone. | Unresolved | Existing controls preserve authority + work state, but superseding a contract is not equivalent to resuming one named decision from captured direction. | Current product: `src/lib/cli/commands/run.mjs#runLifecycle`; Current product: `src/lib/cli/commands/governance/delivery/contract-lifecycle.mjs#ContractLifecycle.transition`; Current product: `src/lib/cli/commands/governance/execution/budget-governance-store.mjs#BudgetGovernanceStore.seal`; Current product: `tests/arcane-package-contract-lifecycle.test.mjs#suspend writes signed receipt` (safeguard inspected, not run) + `tests/arcane-package-budget-governance.test.mjs#role caps & authenticated amendments bind budget changes` | High |
| Legion current product | Work graph & execution coordination | LEG-019 — Enforce per-run step, call, spend & wall-time limits while terminating descendant processes on timeout or cancellation. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. Host events enforce active-time/progress/retry stops; native scheduler enforces deadline/cancellation; external-process runner requests process-tree hard kill. No unified run ledger enforces step + call counts, actual spend + wall time together. | Current live mechanism: authenticated task-budget stop on host-event ingress plus bounded provider/external-process timeout cancellation. | Add one run-bound counter ledger for steps, provider/tool calls, reported token cost + elapsed wall time; reserve before execution, charge actuals after completion, cancel shared token on breach, hard-kill/reap process group, persist terminal stop + cleanup receipt. | Unresolved | Separate controls cover several failure modes, yet independent stores + reservation-only native cost cannot prove one per-run ceiling across all executors. | Current product: `src/lib/host/arcane/host-runtime.mjs#handle`; Current product: `src/lib/cli/commands/governance/execution/budget-governance-store.mjs#BudgetGovernanceStore.observe`; Current product: `engine/crates/legion-runtime/src/scheduler.rs#Scheduler.execute_provider`; Current product: `src/lib/providers/executor/external-process.mjs#runExternal`; Current product: `tests/arcane-package-budget-governance.test.mjs#budget seals authenticate` (safeguard inspected, not run) + `tests/provider-executor.test.mjs#timeout aborts the child and reports timeout` | High |
| Legion current product | Work graph & execution coordination | LEG-020 — Replan remaining dependency work from failure evidence while preserving completed outputs. | change to MISSING; implementation=MISSING; verification=PENDING; qualification=PENDING; delivery=LOCAL. Checkpoint verifier can flag `execution_replan_required` + preserve artifacts, but operative schedulers consume fixed dependency order, mark failed descendants skipped + never construct a replacement plan. | No proven winner | On terminal failure, persist structured failure + completed output digests, compute affected dependency cone, freeze completed nodes, compile replacement DAG only for remaining nodes, bind new schedule fingerprint + acceptance invalidations, then resume through same checkpoint gate. | Unresolved | Replan intent exists as returned metadata only; no production caller turns it into executable remaining work, so observable behavior is absent. | Current product: `src/lib/host/arcane/continuity.mjs#verifyExecutionCheckpoint`; Current product: `engine/crates/legion-runtime/src/scheduler.rs#Scheduler.run`; Current product: `src/lib/core/execute-plan.mjs#executePlan`; Current product: `tests/stage4-continuity.test.mjs#checkpoint validates exact bindings & schedule-only change replans without invalidation` (safeguard inspected, not run) | High |
| Legion current product | Work graph & execution coordination | LEG-021 — Normalize executor actions & return explicit malformed-output guidance for bounded correction. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. Native scheduler normalizes provider results + validates shape; external-process runner emits `malformed-output` when parser fails. Neither path normalizes executor actions into one contract nor returns field-level corrective guidance + bounded retry. | Current live mechanism: provider-result normalization + typed malformed-output terminal state. | Define executor action envelope with canonical operation/effect/target fields; parse + normalize once, return machine-readable error path/expected shape/example, permit one correction only before any effect, then persist corrected action digest or terminal malformed receipt. | Unresolved | Current normalization protects result integrity after execution, while requested contract governs proposed actions before effects + guides one safe correction. | Current product: `engine/crates/legion-provider-sdk/src/result.rs#normalize_result`; Current product: `engine/crates/legion-runtime/src/scheduler.rs#Scheduler.run`; Current product: `src/lib/providers/executor/external-process.mjs#runExternal`; Current product: `tests/provider-executor.test.mjs#runExternal` (safeguard inspected, not run) | High |
| Legion current product | Evidence & delivery | LEG-022 — Preserve an inspectable execution trajectory with parent, dependency, terminal & submission state. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. Authenticated event store persists/replays sequence, execution ID, phase, episode transitions, outputs + terminal reason from live host ingress. Parent, dependency + submission state are absent from accepted-event schema, while plan dependencies live separately. | Current live mechanism: signed append-only architecture trajectory consumed by host runtime. | Extend accepted trajectory with parent execution ID, work-node ID, dependency IDs, submission state/timestamp + terminal state; project plan dependencies into each node trajectory, expose read-only inspect command, preserve hash-chain replay + privacy classes. | Unresolved | Existing trajectory is durable + live but cannot reconstruct requested execution tree or submission lifecycle without joining unrelated plan state. | Current product: `src/lib/verification/arcane/architecture-event-store.mjs#ArchitectureEventStore.accept`; Current product: `src/lib/host/arcane/continuity.mjs#consumeHostArchitectureLifecycle`; Current product: `src/lib/host/arcane/host-runtime.mjs#finish`; Current product: `tests/arcane-package-m7-state-replay.test.mjs#ArchitectureEventStore` (safeguard inspected, not run) + `tests/arcane-package-runtime-binding.test.mjs#ArchitectureEventStore` | High |
| Legion current product | Evidence & delivery | LEG-023 — Attribute model calls, tokens & cost to executions and work units for operator inspection. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. HTTP inference parses provider token usage. Live installed hook dispatch constructs route/cost traces from supported host events, validates them + appends JSONL receipts when required trace fields are supplied; task attribution remains optional. No proven trace exists for every model call, required work-unit attribution is incomplete + no operator inspection/aggregation surface consumes persisted traces. | Current live mechanism: provider-response token parsing plus hook-driven native route/cost trace construction, validation + append-only JSONL persistence. | Emit one immutable usage event per model call bound to execution, task + node IDs; retain provider/model, input/output/total tokens, reported or tariff-derived micro-cost + completeness flag; aggregate through read-only run inspection without estimating missing usage. | Unresolved | Live persistence disproves schema-only telemetry, but emission depends on complete host-supplied route fields rather than proven per-model-call coverage; optional `task_id` does not require work-unit/node attribution + no operator inspection/aggregation consumer was found. | Current product: `engine/crates/legion-provider-sdk/src/http_client.rs#parse_response`; Current product: `engine/crates/legion-provider-sdk/src/inference.rs#InferenceResponse`; Current product: `engine/bins/legion-hook/src/main.rs#dispatch`; Current product: `engine/bins/legion-hook/src/main.rs#emit_route_trace`; Current product: `engine/bins/legion-hook/src/main.rs#route_trace_from_request`; Current product: `engine/bins/legion-hook/src/main.rs#append_trace`; Current product: `hooks/hooks.json`; Current product: `engine/crates/legion-contracts/src/trace.rs#RouteOutcomeTrace`; Current product: `engine/crates/legion-contracts/src/trace.rs#tests` (safeguard inspected, not run) | High |
| Legion current product | Work graph & execution coordination | LEG-024 — Start bound workflows from schedules or external events while preserving trigger metadata. | change to MISSING; implementation=MISSING; verification=PENDING; qualification=PENDING; delivery=LOCAL. `legion schedule` only reads + renders an existing schedule receipt; host runtime records lifecycle hooks but starts no bound workflow from them. No trigger admission, durable trigger identity, dedupe or enqueue path was found. | No proven winner | Add signed trigger envelope with trigger ID/type/source, scheduled time or event digest, received time, target contract/task + idempotency key; validate binding, persist before enqueue, start one workflow, retain trigger metadata in run/trajectory + reject replay. | Unresolved | Static schedule presentation + host telemetry are not workflow activation; treating either as trigger execution would invent reachability. | Current product: `src/lib/cli/commands/schedule.mjs#runSchedule`; Current product: `src/lib/host/arcane/host-runtime.mjs#handle`; Current product: `src/lib/host/arcane/host-event.mjs#normalizeHostEvent`; scoped source search found no trigger-to-workflow consumer | High |
| Legion current product | Work graph & execution coordination | LEG-025 — Stop accepting work & drain active operations before runtime shutdown. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. Native entrypoint maps Ctrl-C to cancellation + command dispatch observes it; scheduler cancels active provider work. Root select returns on cancellation, `serve` receives no cancellation token + no runtime admission/drain state tracks active operations. | Current live mechanism: process-wide Ctrl-C cancellation propagated to current native command/provider context. | Introduce runtime states RUNNING, QUIESCING + STOPPED; atomically reject new submissions in QUIESCING, cancel only interruptible work, await active registry until deadline, hard-kill survivors, flush receipts, then close transport + report drained/forced counts. | Unresolved | Cancellation limits work duration but does not prove admission closure or ordered draining, especially for stdio service. | Current product: `engine/bins/legion/src/main.rs#main`; Current product: `engine/bins/legion/src/cli.rs#run_with_cancellation`; Current product: `engine/bins/legion/src/cli.rs#native_m1_serve`; Current product: `engine/crates/legion-runtime/src/scheduler.rs#Scheduler.execute_provider`; safeguard inspected, not run: inline source tests in `engine/crates/legion-runtime/src/scheduler.rs#tests` | High |
| Legion current product | Evidence & delivery | LEG-026 — Forward execution observations with bounded batching, dead-letter retention & explicit redrive. | change to MISSING; implementation=MISSING; verification=PENDING; qualification=PENDING; delivery=LOCAL. Live host path appends authenticated observations to local receipt/event stores only. No observation forwarder, batch ceiling, durable outbox/dead-letter state or redrive operation was found. | No proven winner | Write accepted observations to durable outbox before acknowledgement; forward batches capped by count + bytes with stable event IDs, retry/backoff + destination receipt; move exhausted items to retained dead-letter ledger, expose inspect/redrive by ID/range, dedupe downstream. | Unresolved | Local hash-chained retention is useful source state but performs none of forwarding contract; calling it partial would collapse storage into delivery. | Current product: `src/lib/host/arcane/host-runtime.mjs#finish`; Current product: `src/lib/verification/arcane/architecture-event-store.mjs#ArchitectureEventStore.accept`; Current product: `src/lib/host/arcane/host-event-ledger.mjs#HostEventLedger.append`; scoped source search found no batch, dead-letter or redrive consumer | High |
| Legion current product | Distribution & host projection | LEG-027 — Publish capability metadata with verifiable identity when available & explicit unavailable trust state otherwise. | change to PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL. Runtime probe publishes summary/degradation/remedy + true/false/null availability through doctor/library consumers; native release binding verifies capability-catalog digest. Probe output itself lacks signed identity/binding digest + explicit trust status. | Current live mechanism: registry-backed capability probe with explicit unavailable/unknown states, separately protected by release catalog digest. | Publish one capability-attestation envelope containing capability ID, metadata digest, availability, trust=`VERIFIED` or `UNAVAILABLE` or `UNKNOWN`, probe evidence, release/catalog identity + signature when verifiable; doctor/API must return same envelope + fail closed on mismatch. | Unresolved | Availability semantics are observable + honest, but verifiable release identity is not carried with each published capability record. | Current product: `src/lib/capabilities/probe.mjs#probeCapability`; Current product: `src/lib/cli/commands/doctor-host.mjs#hostRequirementHealth`; Current product: `src/lib/cli/commands/doctor-host.mjs#computeHostSection`; Current product: `engine/crates/legion-runtime/src/release_binding.rs#verify_release_binding`; Current product: `tests/capability-probe.test.mjs#an absent capability reports unavailable with its remedy` (safeguard inspected, not run) | High |
| Legion current product | Work orchestration | LEG-005 — Compile non-trivial work into dependency-aware work units with capabilities, operations, effects & authority state. | `implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=COMMITTED` — **retain PARTIAL**. Dependency ordering & node-local `ExecutionRequirementV1` are implemented, but live compiler writes `executor_requirement: None`; requirement schema also omits operations. | Current `Plan::validate` / `Plan::ordered_nodes` dependency mechanism plus node-local requirement validation; no proven complete mechanism. | Extend requirement contract with operations; derive capabilities, operations, effects & authority ceiling for every non-trivial node in `compile_plan`; make scheduler consume validated requirement before execution. | Unresolved | Scheduler honors dependencies, but execution semantics are not compiled into live nodes. Existing compatibility projection preserves wire compatibility but cannot fill absent requirements. | Current product: `engine/crates/legion-contracts/src/plan.rs#ExecutionRequirementV1` plus `Plan::validate` & `Plan::ordered_nodes`; `engine/crates/legion-runtime/src/plan.rs::compile_plan` sets every node requirement to `None`; `engine/crates/legion-runtime/src/engine.rs::LegionRuntime::execute` calls compiler; `engine/crates/legion-runtime/src/scheduler.rs::Scheduler::run` consumes ordered dependencies but not requirements. State/fallback — malformed/cyclic plans fail closed; absent requirements remain empty. Safeguard — embedded `engine/crates/legion-contracts/src/plan.rs` requirement, projection & dependency tests, including `accepts_a_well_formed_requirement_and_plan`. | High |
| Legion current product | Native release | LEG-015 — Publish exact signed native candidates only after hosted signing & installed qualification gates. | `implementation=DELIVERED candidate; verification=PENDING; qualification=PENDING; delivery=LOCAL` — **candidate DELIVERED**. Hosted workflow now carries exact unsigned candidates through Windows/macOS signing, Windows installed qualification & tag-only publication; this is implementation evidence only, not fresh qualification or delivery proof. | Current hosted release chain. | Retain digest-bound same-run handoffs, protected release environment, prerequisite gates & post-upload download/digest verification. | No | Publication requires successful Windows installed qualification plus macOS signing; finalization manifests bind source/version/artifact bytes, & publisher re-downloads every GitHub asset for digest verification. No package-manager alias is needed. | Current product: `.github/workflows/release-candidate.yml#jobs` covering `candidate`, `windows-sign`, `macos-sign`, `installed-qualification`, `publish`; `package.json` routes each job to `scripts/release/installer-release-chain.mjs`; live symbols `finalizationManifest`, `qualifyInstalled`, `publishQualified` reject identity/digest/prerequisite drift & verify downloaded assets. State/fallback — any missing signer, mismatched finalization, qualification or asset fails closed before/after upload. Safeguard — `tests/release/installer-release-chain.test.mjs` covers exact finalization, installed qualification & publish/download verification; tests were inspected, not run. | High |
| Legion current product | Audit delivery | LEG-016 — Emit signed & sealed audit plans with SARIF, execution receipts & Blueprint generation pinning. | `implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=LOCAL` — **retain PARTIAL**. Package JavaScript CLI reaches sealed plan, Blueprint binding, provider execution & SARIF finalization; installed native CLI emits signed plan/report/SARIF/execution artifacts but explicitly marks default provider composition partial. | Current package JavaScript audit path. | Bring installed native composition to provider/coverage parity with package runner while retaining native signed plan, generation pin, execution artifact & SARIF output; reject full-Audit claims until parity exists. | Unresolved | JavaScript path implements requested artifact chain. Native default runs only `security.native-rules` & adds `native-provider-composition-partial`, so installed surface cannot expose equivalent full-Audit behavior. | Current product: `src/lib/cli/commands/audit.mjs#runAudit` calls `tools/audit/audit-run.mjs::runAuditProviders`; `tools/audit/audit-complete.mjs::runCompleteAudit` reads Blueprint packet, builds/writes plan & verifies binding; `tools/audit/audit-plan.mjs::sealPlan` / `verifyPlanBinding` bind HMAC, repository & Blueprint generation; `tools/audit/audit-finalize.mjs::finalizeAudit` writes JSON + SARIF. Native live consumer `engine/bins/legion/src/commands/audit.rs::run` writes `report.sarif` + `execution.json` but inserts explicit partial-composition gap. State/fallback — unsigned/unbound/incomplete inputs become coverage gaps or incomplete status. Safeguards — `tests/audit-blueprint-refresh.test.mjs`, `tests/audit-verify.test.mjs`, `tests/audit-pipeline-completion.test.mjs`, `tests/audit-finalize.test.mjs`; tests were inspected, not run. | High |
| Legion current product | Guard evidence | GRD-009 — Record authenticated effect observations & dependency-bound evidence without accepting model self-report. | `implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=COMMITTED` — **retain PARTIAL**. Live host adapters authenticate observed effects & reject model self-report, but production hook runtime does not supply a `DependencyLedger`; completion evidence is sealed with no dependency list. | Current authenticated host-observation path; no proven complete dependency-bound mechanism. | Instantiate one durable dependency ledger in host runtime; register every sealed evidence envelope with lossless dependencies; pass ledger into every `handleHookEvent`; persist invalidation events & make completion eligibility consume resulting freshness. | Unresolved | Authentication, replay defense, correlation & durable effect receipts are live. Dependency model/invalidation exists, but live Codex/Claude dispatch omits ledger wiring, so source changes cannot reliably stale completion evidence through this path. | Current product: `src/lib/guard/compat/audit/receipt-auth.mjs#verifyRecord`; `src/lib/verification/arcane/ingest.mjs::HostIngestor.ingest`; live callers `src/lib/host/arcane/host-runtime.mjs::dispatchHookInvocation` → `handleHookEvent`. `src/lib/verification/arcane/evidence-envelope.mjs::sealEvidence` & `invalidation.mjs::DependencyLedger` implement dependency records/cascade, but host runtime calls omit `dependencyLedger`; `src/lib/cli/commands/completion.mjs::runEvidence` calls `sealEvidence` without dependencies. State/fallback — model self-report, malformed auth, missing correlation or key failure are refused; absent ledger performs no invalidation. Safeguards — `tests/arcane-package-s04-ingest.test.mjs` & `tests/arcane-package-s05-invalidation.test.mjs`; tests were inspected, not run. | High |
| Legion current product | Guard credentials | GRD-013 — Expose provider credentials only inside authorized execution scope & keep credential material out of public receipts. | `implementation=UNKNOWN; verification=PENDING; qualification=PENDING; delivery=LOCAL` — **retain UNKNOWN**. Credential-isolation primitives exist, but scoped production search found no live constructor/consumer connecting `SecretProvider` → `BearerAuth` → `HttpInferenceClient` under Guard authorization. | No proven winner. | Resolve credentials through Guard-authorized `CREDENTIAL_ACCESS` only inside provider execution; construct transport there; expose only `RedactedAuth` metadata; prohibit credential/header material in review receipts & failure strings. | Unresolved | `SecretString`, redacted debug output, reserved authorization/cookie headers & credential-free review receipt schemas are useful groundwork. Without live composition or effect-scope gate, operative behavior cannot be observed or safely upgraded to PARTIAL/DELIVERED. | Current product: `engine/crates/legion-provider-sdk/src/auth.rs#SecretProvider` plus `BearerAuth` & `RedactedAuth`; `engine/crates/legion-provider-sdk/src/http_client.rs::HttpInferenceClient::send` injects bearer auth internally & ignores caller authorization/cookie headers; `engine/crates/legion-review/src/provider.rs::infer_judgment` consumes injected `InferenceClient`; `engine/crates/legion-review/src/receipt.rs::ReviewReceipt` contains provider identity/budget only. Negative reachability evidence — scoped production search found no `HttpInferenceClient` or `BearerAuth::from_provider` construction outside SDK definitions. State/fallback — missing credential maps to typed error, but authorized execution scope is unproven. | Medium |
| Legion current product | Route envelope | ARC-001 — Compile minimum sufficient context, cognition, grounding, compute, challenge, verification & response policy. | retain PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=COMMITTED. Evidence boundary: policy fragments exist, but no compiled route envelope or registered in-lane consumer is proven. | No proven winner | Add one ephemeral `compileArcaneRoute(input, availability)` returning only required context, cognition, grounding, compute, challenge, verification & response fields; call it from registered host ingress before response. | Unresolved | Brief, Minimize, CCX & gotcha assembly covers context policy only. Native registered ingress is outside this lane's readable source, so full runtime reachability remains unproven. | Current product: `src/lib/cognitive/arcane/host/policy-inject.mjs#buildPolicyInjection`; caller candidate `src/lib/host/arcane/host-runtime.mjs#createHostRuntime`; registered ingress `hooks/hooks.json#hooks`; binary identity `src/registry/capabilities.json#legion-hook`; safeguard `tests/arcane-package-policy-inject.test.mjs` not run. | Low |
| Legion current product | Route envelope | ARC-002 — Resolve trivial default route with no model call & near-empty envelope. | change to MISSING; implementation=MISSING; verification=PENDING; qualification=PENDING; delivery=COMMITTED. Evidence boundary: scoped production search found no trivial router, zero-model branch, model-call counter, or near-empty route envelope. | No proven winner | Add synchronous zero-model fast path before normal route compilation; trivial input returns minimal typed envelope with `modelCalls=0`, while nontrivial input falls through to evidence-based routing. | Unresolved | Existing policy injector emits Brief + Minimize by default, opposite near-empty route behavior. No live no-model routing consumer is proven. | Current product: `src/lib/cognitive/arcane/host/policy-inject.mjs#buildPolicyInjection`; scoped search across `src/lib/cognitive`, `src/lib/host`, `src/lib/cli`, `hooks` & `scripts` found no trivial-route mechanism; safeguard `tests/arcane-package-policy-inject.test.mjs` not run. | Low |
| Legion current product | Cognitive controls | ARC-005 — Run at most one evidence-directed bounded falsification pass with KEEP, NARROW, or REVISE result. | retain PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=COMMITTED. Evidence boundary: live native SessionStart transports one-pass KEEP/NARROW/REVISE policy, but no deterministic runner, evidence inspection, invocation state, or enforced result is proven. | Native SessionStart policy transport is observed; no complete falsification mechanism is proven. | Add one-shot falsifier bound to claim + evidence, enforce pass count `<=1`, return typed KEEP/NARROW/REVISE, then feed result into same response route. | Unresolved | Embedded policy defines trigger, evidence direction, one-pass bound, non-recursion & result vocabulary. Native lifecycle delivers it, while execution + result enforcement remain model-policy instructions rather than deterministic runtime behavior. | Current product: `engine/bins/legion-hook/src/main.rs#SESSION_START_CONTEXT`; Current product: `engine/bins/legion-hook/src/main.rs#dispatch_inner`; Current product: `engine/bins/legion-hook/src/main.rs#response_value`; Current product: `hooks/hooks.json#hooks.SessionStart`. | Medium |
| Legion current product | Cognitive controls | ARC-006 — Escalate routing uncertainty to stronger working model without creating workflow ritual. | retain PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=COMMITTED. Evidence boundary: evidence gate exists for attempted `codex exec`, but no route-uncertainty detector, stronger-model selector, or live in-lane host ingress is proven. | No proven winner | After local route attempt remains uncertain, select declared stronger working-model tier, execute once through registered ingress, return result directly to response path & emit no workflow artifact. | Unresolved | Two-failure gate prevents unevidenced dispatch, but it neither detects routing uncertainty nor chooses stronger model. Registered hook invokes native binary outside lane source scope. | Current product: `src/lib/host/arcane/codex-escalation.mjs#evaluateCodexEscalation`; caller candidate `src/lib/host/arcane/hook-adapter-core.mjs#handleHookEvent`; registered ingress `hooks/hooks.json#hooks`; safeguard `tests/arcane-package-codex-escalation.test.mjs` not run. | Low |
| Legion current product | Response policy | ARC-009 — Degrade unavailable optional cognitive machinery while preserving useful permitted work. | retain PARTIAL; implementation=PARTIAL; verification=PENDING; qualification=PENDING; delivery=COMMITTED. Evidence boundary: JS host runtime preserves ambient work on selected availability failures & policy reads skip missing optional inputs, but registered native consumer behavior is unproven in this lane. | No proven winner | Make optional-stage availability explicit in route envelope; skip unavailable optional stages, record ephemeral degradation reasons, preserve ambient permitted response path & fail only required stages. | Unresolved | `availabilityFallback` distinguishes governed denial from ambient degraded continuation, while policy assembly tolerates unreadable optional files. Missing registered JS ingress prevents DELIVERED classification. | Current product: `src/lib/host/arcane/host-runtime.mjs#availabilityFallback`; policy fallback `src/lib/cognitive/arcane/host/policy-inject.mjs#readFileOrNull`; safeguard `src/lib/verification/arcane/semantic-health.mjs#ambient-availability-bypass` & `tests/arcane-package-policy-inject.test.mjs` not run. | Low |

## Disposition

Requested=21; evaluated=21; unresolved=21; excluded=0. Implementation reassessment: PARTIAL=15; MISSING=4; UNKNOWN=1; DELIVERED candidate=1. Candidate delivery does not close LEG-015 because verification, qualification, evidence & required delivery boundary remain pending.

Thirteen implementation classifications have source-supported change candidates: LEG-015, LEG-017–LEG-027 & ARC-002. Eight retain current implementation state: LEG-005, LEG-016, GRD-009, GRD-013, ARC-001, ARC-005, ARC-006 & ARC-009. No atom closes, no new atom is required, & no exclusion is proposed.

## Adversarial reconciliation

Lane reports were reconciled by exact atom ID, not vote. Every positive mechanism retained only with production symbol + live consumer; test references remain safeguards only. Missing classifications require scoped production search plus absent consumer. Lifecycle dimensions remain separate, so implementation evidence does not promote verification, qualification, delivery or closure.
