# Legion Foundation Stage 3 independent comparison packet

## Raw user scope

- Run two independent Sol passes over `\\192.168.1.7\d\claude\repos\legion`, with separate reports & no cross-contamination.
- Capture missed atoms into Legion canon & generated pending as needed.
- Continue Legion lifecycle with implementation comparison, consolidation, gap projection, reuse/license disposition, implementation, verification, closure & document reconciliation.
- Current instruction: `get on with it`.

## Stage contract

Produce one blind Foundation Stage 3 implementation comparison against immutable 109-row Legion canon & frozen 18-donor corpus. Inspect current Legion product using same evidence standard as donors. Do not mutate canon, product code, pending index, corpus, sibling report, or any path except assigned report.

Use `D:/Claude/tools/skills/foundation/SKILL.md`, `references/model.md` & `references/protocol.md`. This packet fixes scope where wording differs.

## Immutable inputs

- Manifest: `D:/Claude/legion/docs/foundation/2026-08-31/legion-stage3-corpus.json`
- Canon files: `D:/Claude/legion/docs/canon/{legion,sage,alchemist,oracle,arcane,guard,covenant,skills}.md`
- Current source root: `D:/Claude/legion`
- Donor roots & exact commits: manifest
- Current source identity: HEAD `ad3d27c6d3d0d04fd7cb850416baa9793db6c9c4` plus operative worktree diff blob `2200f46733c67328e3158cd0062d8a376eb39dba`
- Canon identity: 109 rows plus per-file SHA-256 values in manifest

## Blindness & source boundary

Do not read sibling output, prior Foundation reports, reconciliation, generated pending, preservation maps, other docs, README files, root Markdown, marketing, web pages, issues, benchmark claims, expected taxonomies, or agent artifacts. Allowed Markdown is assigned packet, immutable canon, Foundation method, operative doctrine/skills when those files implement an atom, & narrowly relevant donor skill instructions. Read operative production source; tests only as safeguards tied to production mechanisms. Exclude generated/build/cache/test-only evidence.

Do not run tests, builds, generators, installs, commits, pushes, merges, or expensive checks. Read-only path existence/source searches are allowed. Create assigned report only.

## Required row set

Every `COMMITTED` capability row in 8 canon files appears exactly once: 109 rows. Use:

- `Scope`: `Legion`
- `Domain`: canon owner from row
- `Atom`: stable ID followed by concise observable behavior, preserving ID exactly

Batch by domain with at most 25 atoms. Complete protocol batch checks before next batch.

## Fixed criteria

Correctness, native UX where relevant, local-first privacy, latency, recovery visibility, accessibility, maintainability & testability. Popularity is never evidence.

## Required table

| Scope | Domain | Atom | Current product | Repository mechanisms | Best observed | Best combined | Rationale / tradeoffs | Source evidence |
|---|---|---|---|---|---|---|---|---|

`Current product` must state implementation, verification, qualification & delivery baseline from canon, then cite operative mechanism + live consumer when proven. `Repository mechanisms` must contain exactly one `<br>`-separated entry for every manifest repository, including `legion_current`, each beginning `RepositoryName:` & using `Observed`, `Unclear`, `Not found`, or `N/A` only. Use `N/A` only for proven target/platform incompatibility.

Every `Observed` entry must include target, production file + symbol, live caller/consumer, mechanism, relevant state/persistence/fallback & focused safeguard when present. Every cited path must exist beneath matching manifest root. Use terse source evidence entries formatted `RepositoryName: `path#symbol`` separated by semicolons.

`Best observed` requires proven superiority under fixed criteria; otherwise `No proven winner`. `Best combined` must be atom-specific executable design: primary path, readiness gate, ordered fallback, state transitions, evidence boundary, recovery/failure visibility, privacy/security constraint & proof tests. Label inference.

## Completion

Run one adversarial self-review in place. Check 109 rows, duplicate IDs, all 19 repository cells per row, current-product baselines, evidence paths, semantic fit, consumers, unsupported absences/winners, generic recommendations, copied cells, category leakage & malformed Markdown. Return report path, row count, changed path, blockers & baseline identity. Make no global completeness claim.
