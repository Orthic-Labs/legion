# Corrected M5 provider & external-tool boundary

Continue from immutable commit `1611c431cd734e01903233075b152fdc29b98e98` in an isolated
worktree. Implement & freeze M5 before any dependent M4 Rules, Research, Review, or Audit lane.

Follow `docs/LEGION-CANONICAL-SSOT.md`, `migration/native-rust/PRODUCT-ARCHITECTURE-V2.md`,
`migration/native-rust/MIGRATION-PLAN-V2.md`, M0 semantic/provider freezes, & frozen M2–M7
ownership amendment. Blueprint is unavailable; record typed degradation, continue applicable work,
never modify Membrane or synthesize graph truth.

Correct stale M5 ownership only where existing native effect execution prevents truthful contract
closure: actual bounded argv-only version probing plus policy version/digest/outcome in every
post-policy receipt. Provider SDK must expose one typed external-project-tool seam which delegates
to `legion-effects`; it must not duplicate process execution or accept shell command strings.

Legacy Node/Python runtime paths are read-only evidence until M7. Worker may edit only packet
allowlist, must not commit or push, must use direct Cargo, & must return patch/evidence to sole
integration owner. Denial, timeout, cancellation, output limit, missing/unsealed executable,
unavailable executor, failed process cleanup, & artifact failure remain incomplete receipt-backed
outcomes—never empty success or false PASS.
