# 2026-09-03

## Wrote docs/DOGFOOD.md (Legion daily dogfood/breakage checklist)
- Read-only investigation of installed Legion 0.3.12 at `C:\Users\adrds\AppData\Local\Orthic Labs\Legion\current`.
- Confirmed 0.3b findings on this machine: setup status reports 5 clients installed but hosts show ZERO legion registration (~/.claude/plugins, settings.json enabledPlugins, ~/.claude.json mcpServers.legion, ~/.codex/config.toml [mcp_servers.legion] all absent); ~/.claude/skills -> D:\Claude\tools\skills (dev junction); legion skills "native implementation not connected"; bare `legion serve --stdio` answers initialize (tools: legion_m1_status, legion_m1_invoke) but `--plugin-root` exits 2 (symlink + canonical-skill errors); tasklist/dispatch hard-code /workspace paths; coder dev-relative install.py; no blueprint/omniroute on PATH.
- 27 shipped skills verified present in plugin/skills; 0/27 CLI-loadable (loader broken). 
- UNVERIFIED: setup ledgers/receipts on-disk path; all 0.3.13 fixes (machine still 0.3.12); designer->seo/scripts refs; legion-skill:// links (0 found); scripts/qualification/ path absent in repo (used host-projection.json + setup status instead).
- Did NOT run setup repair/apply/remove (read-only rule).
