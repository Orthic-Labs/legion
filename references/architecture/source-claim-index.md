# Source claim index

Every external claim records source locator, date, represented context, claim, provenance type, evidence grade, limitations, owner, & affected decision IDs. An unverified source is context, never a constraint or acceptance result.
