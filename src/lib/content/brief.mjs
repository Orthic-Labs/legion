export function createContentBrief(input = {}) { const explicit = input.explicit ?? {}; return { schemaVersion: 1, kind: 'legion-content-brief', explicit, inferred: input.inferred ?? {}, status: Object.keys(explicit).length ? 'pass' : 'review-required', coverageGaps: Object.keys(explicit).length ? [] : ['content-intent-missing'] }; }
