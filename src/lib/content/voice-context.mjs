export function createVoiceContext(input = {}) { const terms = Object.entries(input.terms ?? {}).map(([canonical, aliases]) => ({ canonical, aliases: Array.isArray(aliases) ? aliases : [aliases] })); return { schemaVersion: 1, kind: 'legion-voice-context', terms, prohibited: input.prohibited ?? [], authoritative: Boolean(input.source), status: input.source ? 'pass' : 'unproven' }; }
