export function evaluateSelector(selector = {}, projection = {}) { const paths = new Set(projection.files ?? []); return (selector.paths ?? []).every((path) => paths.has(path)) && (selector.extensions ?? []).every((ext) => (projection.parsedExtensions ?? []).includes(ext)); }
