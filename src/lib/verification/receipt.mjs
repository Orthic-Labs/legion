import { digest } from '../core/binding.mjs'; import { semanticProjection } from './projection.mjs';
export function verificationReceipt(prior, current) { const priorDigest = digest(semanticProjection(prior)); const currentDigest = digest(semanticProjection(current)); return { schemaVersion: 1, kind: 'legion-verification-receipt', valid: priorDigest === currentDigest, priorDigest, currentDigest }; }
