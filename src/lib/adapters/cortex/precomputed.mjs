import { sameBinding } from '../../core/binding.mjs';
export function loadPrecomputedProjection(projection, expectedBinding) { if (projection.schemaVersion !== 1) throw new TypeError('unsupported Cortex projection schema'); if (!sameBinding(projection.binding, expectedBinding)) throw new TypeError('Cortex projection binding mismatch'); if (!projection.generation && !projection.manifestDigest) throw new TypeError('Cortex projection generation identity missing'); return projection; }
