export const PLANNING_STAGE_IDS = Object.freeze(['repository-binding','cortex-projection','product-portfolio','component-graph','stack-external-system-graph','release-contract-product-context','control-baseline','evidence-capabilities','scenario-matrices','family-lens-provider-rule-selection','provider-dag','plan-sealing']);
const REQUIRED = Object.freeze({ inventory: ['repository-binding','cortex-projection','product-portfolio'], source: ['repository-binding','cortex-projection','product-portfolio','component-graph','stack-external-system-graph','release-contract-product-context','control-baseline','evidence-capabilities','scenario-matrices'], runtime: PLANNING_STAGE_IDS, product: PLANNING_STAGE_IDS, release: PLANNING_STAGE_IDS });
export async function runPlanningStages(stages, options = {}, host = {}) { const byId = new Map(stages.map((stage) => [stage.id, stage])); const artifacts = {}; const gaps = []; for (const id of PLANNING_STAGE_IDS) { const stage = byId.get(id); if (!stage) { if ((REQUIRED[options.claimLevel ?? 'inventory'] ?? []).includes(id)) gaps.push({ stage: id, status: 'missing' }); continue; } const result = await stage.run({ ...options, artifacts }, host); if (result.artifact) artifacts[id] = result.artifact; if (!result.complete) gaps.push({ stage: id, status: result.status ?? 'unproven', detail: result.detail ?? null }); } return { status: gaps.length ? 'unproven' : 'pass', complete: gaps.length === 0, artifacts, gaps }; }
