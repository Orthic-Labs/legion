export function reconcileDenominator(expected = [], examined = []) { const seen = new Set(examined); return { expected: [...new Set(expected)].sort(), examined: [...seen].sort(), missing: [...new Set(expected)].filter((id) => !seen.has(id)).sort() }; }
