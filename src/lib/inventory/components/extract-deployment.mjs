import { component } from './contracts.mjs';
export function extractDeploymentComponents({ portfolio, projection = {} }) { return (projection.deployments ?? []).map((item) => {const path=item.path??item;return component('infrastructure-dns',item.targetIds??portfolio.targets.map((target)=>target.id),[path],{runtimeBoundary:item.runtimeBoundary??'deployment',privilege:item.privilege??null,identity:item.identity??null,environmentIds:item.environments??[],deploymentKind:item.kind??'unknown'});}); }
