import { component } from './contracts.mjs';
export function extractRuntimeComponents({ portfolio, projection = {} }) {const buildOutputs=new Set((projection.buildOutputs??[]).map((item)=>String(item.path??item).replaceAll('\\','/')));return (projection.entrypoints ?? []).filter((item)=>typeof item==='object'||!buildOutputs.has(String(item).replaceAll('\\','/'))).map((item) => {const path=item.path??item;const kind=/worker|job|queue/i.test(path)?'queue-job':/ipc|bridge/i.test(path)?'native-bridge-ipc':'helper-sidecar';return component(kind,item.targetIds??portfolio.targets.map((target)=>target.id),[path],{runtimeBoundary:item.runtimeBoundary??'process',privilege:item.privilege??null,identity:item.identity??null,environmentIds:item.environments??[]});}); }
