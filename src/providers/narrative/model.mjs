export function buildNarrativeModel(items = []) { const units = items.map((item, index) => ({ id: item.id, order: index, text: item.text, claimRefs: item.claimRefs ?? [], purpose: item.purpose ?? null })); return { schemaVersion: 1, kind: 'legion-narrative-model', units, complete: units.every((unit) => unit.id && unit.text), coverageGaps: units.filter((unit) => !unit.purpose).map((unit) => `purpose-missing:${unit.id}`) }; }
