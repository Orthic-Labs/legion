// Compatibility import path for generation scripts and their tests. Canonical
// parsing lives in src/ so runtime closure checks use identical semantics.
export { parseSkillFrontmatter } from '../../src/lib/skills/skill-frontmatter.mjs';
