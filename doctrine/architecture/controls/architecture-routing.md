# Architecture routing

`architecture-router-input.v1` accepts declared facts only. Normalize documented aliases at boundary, then persist only schema-defined snake_case values. Objective changes after first route are rejected: an attractive redesign is an informational ceiling, never a self-upgrade.

Router emits `architecture-route.v1`: significance result, matched facts, objective, depth, rigor, & effect classification. Classification names an authority boundary; it grants none.
