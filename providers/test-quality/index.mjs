const TYPES={missing:'missing-coverage',weak:'weak-oracle',fake:'fake-completion'};export function classifyTestGap(value){return TYPES[value]??'unproven-pattern';}export function testQualityFinding(value){if(!value.evidencePath||!value.contract)throw new Error('test-quality evidence and contract required');return{...value,kind:classifyTestGap(value.kind),adjudicationRequired:Boolean(value.subjective)};}
export function analyze({artifacts}={}){const observations=artifacts?.testQuality??[];const findings=[];const gaps=[];for(const observation of observations){try{findings.push(testQualityFinding(observation));}catch(error){gaps.push({kind:'test-quality-evidence-invalid',reason:error.message});}}if(!observations.length)gaps.push({kind:'test-quality-denominator-zero'});return{status:gaps.length?'unproven':findings.length?'fail':'pass',complete:gaps.length===0,denominator:{kind:'test-quality-observations',expected:observations.length,examined:findings.length},findings,coverageGaps:gaps};}
