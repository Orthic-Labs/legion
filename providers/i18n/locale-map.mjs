export function buildLocaleMap({ defaultLocale, locales = [], surfaces = [] } = {}) { const missing = surfaces.flatMap((surface) => locales.filter((locale) => !surface.locales?.includes(locale)).map((locale) => ({ surfaceId: surface.id, locale }))); return { schemaVersion: 1, kind: 'legion-locale-map', defaultLocale: defaultLocale ?? null, locales, missing, status: missing.length ? 'partial' : 'pass' }; }
