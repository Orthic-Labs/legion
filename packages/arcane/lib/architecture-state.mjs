import { ArcaneError } from './errors.mjs';
import { digestValue } from './canonical.mjs';
export const ARCHITECTURE_STATE_SCHEMA_ID='architecture-state.v4';
export const ARCHITECTURE_STATUS=Object.freeze(['UNROUTED','TAILORED','FRAMED','DRIVERS_READY','CANDIDATES_READY','EVALUATED','MINIMIZED','DECIDED','CHALLENGED','FROZEN','EVIDENCE_TASK','EXECUTING','VERIFIED','FAILED','BLOCKED']);
export const EXECUTION_EPISODE_STATE=Object.freeze(['PENDING','QUEUED','RUNNING','SUCCEEDED','FAILED','CANCELLED','TIMEOUT','BUDGET_STOP','COMPLETE_WITH_DEBT']);
export const INVALIDATION_TARGET=Object.freeze({PATCH:'same',PLAN:'FROZEN',DESIGN:'CANDIDATES_READY',ROOT:'TAILORED'});
export const ARCHITECTURE_TRANSITIONS=Object.freeze({UNROUTED:['TAILORED'],TAILORED:['FRAMED'],FRAMED:['DRIVERS_READY'],DRIVERS_READY:['CANDIDATES_READY'],CANDIDATES_READY:['EVALUATED'],EVALUATED:['MINIMIZED'],MINIMIZED:['DECIDED'],DECIDED:['CHALLENGED','FROZEN'],CHALLENGED:['FROZEN'],FROZEN:['EVIDENCE_TASK','EXECUTING'],EVIDENCE_TASK:['FROZEN','EXECUTING','FAILED','BLOCKED'],EXECUTING:['VERIFIED','FAILED','BLOCKED']});
export const EXECUTION_EPISODE_TRANSITIONS=Object.freeze({PENDING:['QUEUED'],QUEUED:['RUNNING'],RUNNING:['SUCCEEDED','FAILED','CANCELLED','TIMEOUT','BUDGET_STOP','COMPLETE_WITH_DEBT']});
function fail(message,detail={}) { throw new ArcaneError('ARC_SCHEMA_INVALID',message,detail); } function clone(v){return structuredClone(v);} function sortStable(value){ if(Array.isArray(value)) return value.map(sortStable).sort((a,b)=> typeof a?.id==='string'&&typeof b?.id==='string'?a.id.localeCompare(b.id):0); if(value&&typeof value==='object'){return Object.fromEntries(Object.entries(value).map(([k,v])=>[k,sortStable(v)]));} return value; }
const TOP_FIELDS=['schema','task','mandate','intent','acceptance_ledger','context','architecture','uncertainty','decision','evidence','assurance','description','convergence','execution','integration','evidence_reachability','gate_validity','machinery_defects','state_fingerprint'];
const BUDGET_SNAPSHOT_FIELDS=['id','budget_ref','budget_digest','observed_counters'];
const BUDGET_COUNTER_FIELDS=['active_time_ms','excluded_wait_ms','retry_count','event_count'];
function exactKeys(value,fields,label){if(!value||typeof value!=='object'||Array.isArray(value))fail(`${label} must be an object`);for(const key of Object.keys(value))if(!fields.includes(key))fail(`${label} contains unknown field: ${key}`);for(const key of fields)if(!Object.hasOwn(value,key))fail(`${label}.${key} is required`);}
function payloadKeys(payload,fields,label){if(!payload||typeof payload!=='object'||Array.isArray(payload))fail(`${label} payload must be an object`);for(const key of Object.keys(payload))if(!fields.includes(key))fail(`${label} payload contains unknown field: ${key}`);}
export function fingerprintArchitectureState(state){const copy=clone(state);delete copy.state_fingerprint;delete copy.execution?.trajectory?.replay_state_fingerprint;delete copy.execution?.trajectory?.last_event_digest;return digestValue(copy);}
export function createArchitectureState({objective_lineage_id,acceptance_ledger,budget_ref}={}) { if(typeof objective_lineage_id!=='string'||!objective_lineage_id)fail('objective_lineage_id is required'); if(!acceptance_ledger||typeof acceptance_ledger!=='object')fail('acceptance_ledger is required'); if(typeof budget_ref!=='string'||!budget_ref)fail('budget_ref is required'); const state={schema:ARCHITECTURE_STATE_SCHEMA_ID,task:{objective_lineage_id,architecture_status:'UNROUTED',budget_ref},mandate:{},intent:{intent_epoch:1,continuation_epoch:1,outcomes:[],success_signals:[],unacceptable_losses:[]},acceptance_ledger:sortStable(acceptance_ledger),context:{route:null},architecture:{},uncertainty:{},decision:{items:[],fingerprints:[],refs:[]},evidence:{items:[],lifecycle:[],artifact_envelopes:[],fingerprints:[],refs:[]},assurance:{findings:[],fingerprints:[],refs:[]},description:{},convergence:{},execution:{episode_state:'PENDING',trajectory:{last_sequence:0,last_event_digest:null,replay_state_fingerprint:null},budget_snapshots:[],retry:{items:[],fingerprints:[],refs:[]},diagnosis:null,delivery_deficits:[],downstream_acknowledgements:[],effects:[],terminal_states:[],recovery_refs:[],supersession_refs:[],findings:[],evidence:[]},integration:{ownership_dispositions:[],migration_cutover:null},evidence_reachability:{},gate_validity:{},machinery_defects:{out_of_scope:[]},state_fingerprint:null};state.state_fingerprint=fingerprintArchitectureState(state);return Object.freeze(state);}
function upsert(target,item){if(!item||typeof item.id!=='string'||!item.id)fail('stable item id is required');const index=target.findIndex((entry)=>entry.id===item.id);if(index>=0)target[index]=sortStable(item);else target.push(sortStable(item));target.sort((a,b)=>a.id.localeCompare(b.id));}
export function applyArchitectureEvent(state,event){ if(!state||state.schema!==ARCHITECTURE_STATE_SCHEMA_ID)fail('invalid architecture state'); if(!event||typeof event!=='object')fail('event is required'); const next=clone(state);const payload=event.payload??{};if(event.event_type==='ROUTE_CLASSIFIED'){next.context.route=sortStable(payload);}else if(event.event_type==='ARCHITECTURE_TRANSITIONED'){payloadKeys(payload,['from','to'],'ARCHITECTURE_TRANSITIONED');const to=payload.to;if(payload.from!==next.task.architecture_status||!ARCHITECTURE_TRANSITIONS[next.task.architecture_status]?.includes(to))fail('illegal architecture transition');next.task.architecture_status=to;}else if(event.event_type==='INVALIDATION_RECORDED'){payloadKeys(payload,['scope','cause','root_evidence'],'INVALIDATION_RECORDED');const {scope,cause}=payload;if(!INVALIDATION_TARGET[scope]||typeof cause!=='string'||!cause)fail('invalidation requires cause and scope');if(scope==='ROOT'&&!payload.root_evidence)fail('root invalidation requires root evidence');if(scope!=='PATCH')next.task.architecture_status=INVALIDATION_TARGET[scope];next.convergence.invalidation={scope,cause,root_evidence:payload.root_evidence??null};}else if(event.event_type==='EXECUTION_EPISODE_TRANSITIONED'){payloadKeys(payload,['from','to'],'EXECUTION_EPISODE_TRANSITIONED');if(payload.from!==next.execution.episode_state||!EXECUTION_EPISODE_TRANSITIONS[next.execution.episode_state]?.includes(payload.to))fail('illegal execution episode transition');next.execution.episode_state=payload.to;}else if(event.event_type==='INTENT_EPOCH_ADVANCED'){payloadKeys(payload,['intent_epoch'],'INTENT_EPOCH_ADVANCED');if(payload.intent_epoch!==next.intent.intent_epoch+1)fail('intent epoch must be contiguous');next.intent.intent_epoch=payload.intent_epoch;next.acceptance_ledger.intent_epoch=payload.intent_epoch;}else if(event.event_type==='CONTINUATION_EPOCH_ADVANCED'){payloadKeys(payload,['continuation_epoch'],'CONTINUATION_EPOCH_ADVANCED');if(payload.continuation_epoch!==next.intent.continuation_epoch+1)fail('continuation epoch must be contiguous');next.intent.continuation_epoch=payload.continuation_epoch;}else {const paths={DECISION_RECORDED:['decision','items'],DECISION_FROZEN:['decision','items'],FINDING_UPSERTED:['assurance','findings'],RETRY_RECORDED:['execution','retry','items'],DELIVERY_DEFICIT_UPSERTED:['execution','delivery_deficits'],DOWNSTREAM_ACKNOWLEDGEMENT_RECORDED:['execution','downstream_acknowledgements'],OWNERSHIP_DISPOSITION_RECORDED:['integration','ownership_dispositions'],ARTIFACT_ENVELOPE_RECORDED:['evidence','artifact_envelopes'],EVIDENCE_LIFECYCLE_RECORDED:['evidence','lifecycle'],EFFECT_RECORDED:['execution','effects'],TERMINAL_STATE_RECORDED:['execution','terminal_states'],SUPERSESSION_RECORDED:['execution','supersession_refs']};if(event.event_type==='BUDGET_SNAPSHOT_RECORDED'){payloadKeys(payload,BUDGET_SNAPSHOT_FIELDS,'BUDGET_SNAPSHOT_RECORDED');exactKeys(payload.observed_counters,BUDGET_COUNTER_FIELDS,'budget observed_counters');if(typeof payload.id!=='string'||!payload.id||typeof payload.budget_ref!=='string'||!payload.budget_ref||!/^sha256:[0-9a-f]{64}$/.test(payload.budget_digest)||Object.values(payload.observed_counters).some((value)=>!Number.isInteger(value)||value<0))fail('budget snapshot requires authenticated reference, digest, and non-negative observed counters');upsert(next.execution.budget_snapshots,payload);}else if(event.event_type==='FINGERPRINTS_RECORDED'){payloadKeys(payload,['decision','evidence','finding','retry'],'FINGERPRINTS_RECORDED');if(payload.decision)next.decision.fingerprints=sortStable(payload.decision);if(payload.evidence)next.evidence.fingerprints=sortStable(payload.evidence);if(payload.finding)next.assurance.fingerprints=sortStable(payload.finding);if(payload.retry)next.execution.retry.fingerprints=sortStable(payload.retry);}else if(event.event_type==='MIGRATION_CUTOVER_RECORDED')next.integration.migration_cutover=sortStable(payload);else if(event.event_type==='ACCEPTANCE_RESULT_RECORDED')upsert(next.acceptance_ledger.items??(next.acceptance_ledger.items=[]),payload);else {const path=paths[event.event_type];if(!path)fail('unsupported architecture event');let list=next;for(const key of path)list=list[key];upsert(list,payload);}}next.state_fingerprint=fingerprintArchitectureState(next);return Object.freeze(next);}
export function projectArchitectureEvents(events, {initial_state}={}){if(!initial_state)fail('initial_state is required');return events.reduce((state,event)=>applyArchitectureEvent(state,event),initial_state);}
export const stateFingerprint=fingerprintArchitectureState;export const reduceArchitectureState=applyArchitectureEvent;export const withStateFingerprint=(state)=>Object.freeze({...clone(state),state_fingerprint:fingerprintArchitectureState(state)});export function validateArchitectureState(state){try{exactKeys(state,TOP_FIELDS,'architecture state');exactKeys(state.task,['objective_lineage_id','architecture_status','budget_ref'],'task');exactKeys(state.intent,['intent_epoch','continuation_epoch','outcomes','success_signals','unacceptable_losses'],'intent');exactKeys(state.acceptance_ledger,['schema','ledger_version','intent_epoch','acceptance_fingerprint','frozen_at','items'],'acceptance_ledger');exactKeys(state.decision,['items','fingerprints','refs'],'decision');exactKeys(state.evidence,['items','lifecycle','artifact_envelopes','fingerprints','refs'],'evidence');exactKeys(state.assurance,['findings','fingerprints','refs'],'assurance');exactKeys(state.execution,['episode_state','trajectory','budget_snapshots','retry','diagnosis','delivery_deficits','downstream_acknowledgements','effects','terminal_states','recovery_refs','supersession_refs','findings','evidence'],'execution');exactKeys(state.execution.trajectory,['last_sequence','last_event_digest','replay_state_fingerprint'],'execution.trajectory');exactKeys(state.execution.retry,['items','fingerprints','refs'],'execution.retry');for(const snapshot of state.execution.budget_snapshots){exactKeys(snapshot,BUDGET_SNAPSHOT_FIELDS,'budget snapshot');exactKeys(snapshot.observed_counters,BUDGET_COUNTER_FIELDS,'budget observed_counters');if(typeof snapshot.id!=='string'||!snapshot.id||typeof snapshot.budget_ref!=='string'||!snapshot.budget_ref||!/^sha256:[0-9a-f]{64}$/.test(snapshot.budget_digest)||Object.values(snapshot.observed_counters).some((value)=>!Number.isInteger(value)||value<0))fail('invalid budget snapshot');}exactKeys(state.integration,['ownership_dispositions','migration_cutover'],'integration');if(state.schema!==ARCHITECTURE_STATE_SCHEMA_ID||state.acceptance_ledger.schema!=='acceptance-ledger.v1')fail('invalid schema');if(!ARCHITECTURE_STATUS.includes(state.task.architecture_status)||!EXECUTION_EPISODE_STATE.includes(state.execution.episode_state))fail('invalid state enum');if(!Number.isInteger(state.intent.intent_epoch)||state.intent.intent_epoch<1||!Number.isInteger(state.intent.continuation_epoch)||state.intent.continuation_epoch<1||state.acceptance_ledger.intent_epoch!==state.intent.intent_epoch)fail('invalid intent epoch');if(!Number.isInteger(state.execution.trajectory.last_sequence)||state.execution.trajectory.last_sequence<0)fail('invalid trajectory sequence');if(state.state_fingerprint!==fingerprintArchitectureState(state))fail('state fingerprint mismatch');return Object.freeze({valid:true,issues:Object.freeze([])});}catch(error){return Object.freeze({valid:false,issues:Object.freeze([error.message])});}}export function assertArchitectureState(state){const r=validateArchitectureState(state);if(!r.valid)fail(r.issues[0]);return state;}
