interface:
  display_name: "Debugger"
  short_description: "Diagnose and repair known failures by reproducing them, isolating evidence, testing disconfirmable hypotheses, establishing root cause, and verifying an authorized routine fix. Do not use for preflight or completion-only verification."
  default_prompt: "Use $debugger when this request matches: Diagnose and repair known failures by reproducing them, isolating evidence, testing disconfirmable hypotheses, establishing root cause, and verifying an authorized routine fix. Do not use for preflight or completion-only verification."
policy:
  allow_implicit_invocation: true
