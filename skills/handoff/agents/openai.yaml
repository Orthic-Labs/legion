interface:
  display_name: "Handoff"
  short_description: "Transfer chat state through bound transcripts"
  default_prompt: "Use $handoff to emit this source task's bound transcript pointer; target chat requests typed Membrane continuity context then validates a cold-start packet."
