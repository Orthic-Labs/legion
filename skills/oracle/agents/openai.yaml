interface:
  display_name: "Oracle"
  short_description: "Independent read-only Completion Validation against the raw user request. Use /oracle before successful delivery."
  default_prompt: "Use $oracle when this request matches: Independent read-only Completion Validation against the raw user request. Use /oracle before successful delivery."
policy:
  allow_implicit_invocation: false
